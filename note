import numpy  # 程序一开始就导入numpy
def some_function():
    import numpy  # 只在需要时才导入,动态导入
    #库比较大，不是每次都需要；根据不同情况导入
# 1.1.4 虚拟环境
 #不同项目需要不同的版本的库
 #为每个项目创建独立的环境
 #python -m venv my_project_env
 #my_project_env\Scripts\activate
 #退出环境deactivate
#1.2没怎么看明白
#1.3 git
#   
